import { NotFoundScreen } from '@widgets/404';

const NotFound = () => {
   return (
      <div className={`page flex flex-col`}>
         <NotFoundScreen />
      </div>
   );
};

export default NotFound;
